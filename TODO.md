# 开发路线图

本路线图按优先级排列。数据安全和实机验收优先于新增功能；完成项目需同时更新 README、中文本地化和回归测试。

## 已完成：构建与测试基础

- [x] 优化 XCTest 工作流耗时：缓存 Swift Package 和构建产物，将依赖解析、测试编译与执行拆分，并上传 `.xcresult`。
- [x] XCTest 并行测试评估：测试使用 UUID 临时仓库和独立状态对象，并以两个 worker 保守启用并行测试。

## 已完成：Hugo 编辑体验

- [x] 管理文章 `images/`：缩略图、插入、重命名、替换和带引用提醒的删除。
- [x] 在当前光标或选区应用 Markdown 格式。
- [x] 支持未保存退出确认、撤销/重做、iPad 分屏和本地图片预览。
- [x] 增强文章列表和 Front Matter：展示标题、日期、草稿状态，并支持筛选和封面字段。
- [x] 一键推送前检查缺失图片引用和超大图片。
- [x] 新建文章时实时显示英文目录、目标重复和模板有效性。
- [x] 自动合并重复仓库后显示清理数量，明确未删除本地文件。
- [x] 文章成功推送后刷新当前文章目录状态；无可提交变更时禁用“保存、提交并推送”。
- [x] 修复系统照片选择入口，并让图片管理识别 BMP、TIFF 等已支持的文章图片。

## 已完成：P0 SideStore 实机验收

- [x] 覆盖安装最新 `GitSync-md-SideStore-*`，确认原仓库配置仍存在且历史重复项已合并。
- [x] 编辑 `index.md`，等待底部依次显示“正在保存草稿”和“草稿已保存在本地”。
- [x] 不保存并退出 App；重新打开文章，在恢复提示中分别验证“恢复草稿”和“使用已保存文件”。
- [x] 使用“保存、提交并推送”，确认仅当前文章目录及 `images/` 被新增暂存。
- [x] 在 GitHub 检查提交内容；断网或令牌失败时确认文件和草稿均未丢失。
- [x] 验证新版 Debug Log 的实时刷新、筛选、复制、分享、清除和重启后恢复。
- [x] 连续运行并行 XCTest，确认两个 worker 不产生偶发失败并记录实际耗时。
- [x] 2026-08-16：XCTest #22 与 SideStore IPA #17 构建通过，P0 实机验收完成。

## 已完成：P1 文章发布状态管理

- [x] 在文章列表和编辑器中提供明确的草稿/发布切换，并显示当前状态。
- [x] 支持设置、修改和清除发布日期，保留模板使用的日期格式与时区。
- [x] 发布前汇总检查 Front Matter、缺失图片、超大图片和未保存草稿。
- [x] 推送失败时保留编辑内容、草稿和暂存选择，并提供可执行的重试提示。
- [x] 为发布状态解析、日期更新和失败恢复补充 XCTest。

## 已完成：P2 Hugo 编辑体验

- [x] 提供 Front Matter 字段配置，让不同站点选择需要展示的自定义字段。
- [x] 支持移动或重命名文章目录，并同步修正文章内的相对图片引用。
- [x] 增加文章搜索排序选项：标题、目录、发布日期、修改时间和草稿状态。
- [x] 增加 App 内文章预览，渲染 Markdown 正文、标题和常用 Front Matter。
- [x] 预览支持文章同级 `images/` 中的相对图片、链接、代码块、表格和基础 Hugo shortcode 占位提示。
- [x] 支持编辑/预览切换及 iPad 分屏实时刷新，并明确显示尚未保存的预览内容。
- [x] 提供与纸张写作界面一致的默认预览样式；后续再评估读取站点 CSS 的主题近似预览。

## P3：Git 稳定性与自动化

- [x] 建立实机回归矩阵，覆盖公开/私有仓库、HTTPS/SSH、Git LFS、分支、stash、tag 和冲突解决。
- [x] 为长时间 clone、pull 和 push 增加更细的阶段进度、取消入口及安全重试。
- [ ] 区分认证失败、网络中断、远程拒绝和仓库损坏，并给出对应处理建议。
- [ ] 评估后台拉取和快捷指令自动同步，明确 iOS 后台执行限制与数据保护策略。

## P4：Hugo 主题预览（后期）

- [ ] 读取 `hugo.toml`、`hugo.yaml` 或 `config/` 中的站点配置，识别主题、语言、永久链接和资源目录。
- [ ] 在隔离的 `WKWebView` 中生成文章预览页面，加载仓库内主题 CSS、字体、图片和静态资源。
- [ ] 建立常用 Hugo 模板变量与 shortcode 的兼容层；无法解析的组件应显示明确占位，不静默丢失内容。
- [ ] 支持在文章使用的布局、内容类型和多语言版本之间切换，并提供手机、平板和桌面宽度预览。
- [ ] 检测主题文件变化并刷新预览，同时限制脚本、外部请求和文件访问范围。
- [ ] 建立示例站点快照测试，对比主题预览与真实 Hugo 构建结果，标注尚不兼容的模板能力。
- [ ] 评估受控集成 Hugo 可执行能力，仅在体积、签名、许可证和 iOS 沙箱限制可接受时提供“精确预览”模式。

## 暂不计划

- 当前阶段不内嵌完整 Hugo 可执行文件或启动长期运行的 Hugo Server；P2 先实现原生 Markdown 预览，P4 再推进主题兼容和可选精确预览。
- 在 GitHub Pages 中存储 OAuth 客户端密钥或访问令牌。
